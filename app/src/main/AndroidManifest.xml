<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.veciconecta">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Veciconecta">

        <!-- Activity principal (Launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity android:name=".LoginActivity" />

        <!-- Registro -->
        <activity android:name=".RegistroActivity" />

        <!-- Menú principal -->
        <activity android:name=".MenuPrincipalActivity" />

        <!-- Pagos de aportes y multas -->
        <activity android:name=".PagosAportesActivity" />

        <!-- Pagos de agua -->
        <activity android:name=".PagosAguaActivity" />

        <!-- Calendario / Noticias -->
        <activity android:name=".CalendarioActivity" />

    </application>

</manifest>
